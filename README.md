# oppiro_game_web

このリポジトリには、Flutter Web プロジェクトのビルド成果物が含まれています。

## 更新を適用するには

`flutter_service_worker.js` などのファイルを変更した後は、サービスワーカーやその他のアセットを再生成するために次のコマンドを実行します。

```bash
flutter build web
```

その後、このディレクトリの内容をデプロイすれば、更新されたアプリケーションを公開できます（例: GitHub Pages など）。
